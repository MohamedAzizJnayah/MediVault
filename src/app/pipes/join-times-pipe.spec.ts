import { JoinTimesPipe } from './join-times-pipe';

describe('JoinTimesPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinTimesPipe();
    expect(pipe).toBeTruthy();
  });
});
